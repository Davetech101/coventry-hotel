import styled from "styled-components";

const SWwa = styled.section`
  padding: 5rem 15rem;
  background: #f8f9fa;

  h2 {
    font-size: 3rem;
    color: #1b361b;
  }

  > p{
    font-size: 1.8rem;
    margin:2rem 0;
  }

  main > div {
    background: #fff;
  }
`;

export default SWwa;
