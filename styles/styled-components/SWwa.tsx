import styled from "styled-components";

const SWwa = styled.section`
  padding: 5rem 15rem;

  h2 {
    font-size: 3rem;
    color: #1b361b;
  }

  > p{
    font-size: 1.8rem;
    margin:2rem 0;
  }
`;

export default SWwa;
